#!/bin/bash
if [ ! -d "/home/coder/workspace/project" ]
then
ssh-keyscan -t rsa github.com >> ~/.ssh/known_hosts
git clone git://calebtonn/terraform-image.git /home/coder/workspace/project
else
echo "Project has already been cloned."
fi
